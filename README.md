# StudentManagementSystem
The Student Management System is a RESTful Web API built using ASP.NET Core that enables efficient management of student records. It provides core CRUD operations such as creating, retrieving, updating, and deleting student data, along with secure access using JWT-based authentication.
